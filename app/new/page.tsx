"use-client";

import { ConfigureFlashCardForm } from "../components/ConfigureFlashcardForm";

const CreateNewFlashcardPage = () => {
  return (
    <div>
      Add a new flashcard
      <ConfigureFlashCardForm />
    </div>
  );
};

export default CreateNewFlashcardPage;
